import { OrderCustomSoftwareEntity } from './order-custom-software.entity';

describe('OrderCustomSoftwareEntity', () => {
  it('should be defined', () => {
    expect(new OrderCustomSoftwareEntity()).toBeDefined();
  });
});
