import { OrderSoftwareEntity } from './order-software.entity';

describe('OrderSoftwareEntity', () => {
  it('should be defined', () => {
    expect(new OrderSoftwareEntity()).toBeDefined();
  });
});
